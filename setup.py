from setuptools import setup, find_packages

setup(
        name='DMCTeamsConnector', 
        version='1.0', 
        install_requires = [
            # 'pycryptodome',
        ],
        packages=find_packages())